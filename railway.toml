[build]
builder = "RAILPACK"
buildCommand = "pnpm install --frozen-lockfile"

[deploy]
startCommand = "pnpm --filter @trinity/seller start"
healthcheckPath = "/merchants"
restartPolicyType = "ON_FAILURE"
